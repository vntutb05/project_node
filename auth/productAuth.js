const {check} = require('express-validator');

let addValidator = ()=>{
    return [ 

    ]
}
let editValidator = ()=>{
    return [
        
    ]
}
module.exports ={
    addValidator :addValidator,
    editValidator :editValidator
}