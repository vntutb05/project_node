<div class="container-fluid">
    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">Add Users</h6>
        </div>
        <%-include("../partials/message")%>
        <div class="col-12">
            <form action="/admin/cate/add" class="formReg" method="POST">
                <div class="form-group">
                    <label for="name">Name</label>
                    <input type="text" placeholder="Input your name" class="form-control" id="name" name="name" value="<%= (typeof(data)=='undefined')? '' :data.name %>">
                </div>
                <div class="form-group">
                    <label for="description">Description</label>
                    <input type="text" placeholder="Input your name" class="form-control" id="description" name="description" value="<%= (typeof(data)=='undefined')? '' :data.description %>">
                </div>
                <button type="submit" class="btn btn-primary  btn-user btn-block mt-2">Lưu</button>
            </form>
        </div>
    </div>
</div>